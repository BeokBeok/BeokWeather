<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="weather_item"
            type="com.example.beokweather.main.model.WeatherItem" />

        <variable
            name="vm"
            type="com.example.beokweather.main.MainViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="12dp">

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cv_main_current_container"
            android:layout_width="0dp"
            android:layout_height="360dp"
            app:cardCornerRadius="16dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@android:color/holo_blue_dark"
                app:layout_constraintDimensionRatio="360:360">

                <androidx.appcompat.widget.AppCompatImageView
                    android:id="@+id/iv_main_weather_icon"
                    android:layout_width="160dp"
                    android:layout_height="160dp"
                    android:layout_marginStart="24dp"
                    android:layout_marginTop="40dp"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    bind:setWeatherIcon="@{weather_item.icon}"
                    tools:src="@drawable/ic_weather_01d" />

                <androidx.appcompat.widget.AppCompatTextView
                    android:id="@+id/tv_main_current_area"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@{weather_item.name}"
                    android:textColor="@android:color/white"
                    android:textSize="28sp"
                    app:layout_constraintEnd_toEndOf="@id/tv_main_current_temp"
                    app:layout_constraintStart_toStartOf="@id/tv_main_current_temp"
                    app:layout_constraintTop_toTopOf="@id/iv_main_weather_icon"
                    tools:text="서울시" />

                <androidx.appcompat.widget.AppCompatTextView
                    android:id="@+id/tv_main_current_temp"
                    android:layout_width="wrap_content"
                    android:layout_height="0dp"
                    android:gravity="center"
                    android:text="@{weather_item.temp}"
                    android:textColor="@android:color/white"
                    android:textSize="84sp"
                    app:layout_constraintBottom_toBottomOf="@id/iv_main_weather_icon"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toEndOf="@id/iv_main_weather_icon"
                    app:layout_constraintTop_toTopOf="@id/iv_main_weather_icon"
                    tools:text="35°" />

                <androidx.appcompat.widget.AppCompatTextView
                    android:id="@+id/tv_main_min_temp"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:gravity="center"
                    android:text="@{@string/label_low_temp(weather_item.tempMin == null ? `` : weather_item.tempMin)}"
                    android:textColor="@android:color/white"
                    android:textSize="40sp"
                    app:layout_constraintEnd_toEndOf="@id/iv_main_weather_icon"
                    app:layout_constraintStart_toStartOf="@id/iv_main_weather_icon"
                    app:layout_constraintTop_toBottomOf="@id/iv_main_weather_icon"
                    tools:text="최저\n30°" />

                <androidx.appcompat.widget.AppCompatTextView
                    android:id="@+id/tv_main_max_temp"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:gravity="center"
                    android:text="@{@string/label_high_temp(weather_item.tempMax == null ? `` : weather_item.tempMax)}"
                    android:textColor="@android:color/white"
                    android:textSize="40sp"
                    app:layout_constraintEnd_toEndOf="@id/tv_main_current_temp"
                    app:layout_constraintStart_toStartOf="@id/tv_main_current_temp"
                    app:layout_constraintTop_toBottomOf="@id/tv_main_current_temp"
                    tools:text="최고\n40°" />

            </androidx.constraintlayout.widget.ConstraintLayout>

        </com.google.android.material.card.MaterialCardView>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>